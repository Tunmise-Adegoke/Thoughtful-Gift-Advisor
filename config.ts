
export const APP_CONFIG = {
  // The Gemini API Key is loaded from the environment variable.
  // Ensure this is set in your deployment platform (e.g., Google Cloud, Vercel, Netlify).
  GEMINI_API_KEY: process.env.API_KEY
};
